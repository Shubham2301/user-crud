requirements:

1. create dB user-crud
2. tables:
	users
		columns:
			name (input)
			email (input)
			phone_number (input)
			gender (radio)
			city (select)
3. APIs
	create
	updated
	delete
	read


4. UI
	create and update form
	list
	deletion